{% extends '_layouts/global' %}

{# Header Colour #}

{# The Header follows the page's Hero Layout (ADR-0004), so an editor never sets it. #}

{% set headerColour = 'black' %}

{% block site %}

    {% include '_components/heroCaseStudy' with {
        params: {
            entry: entry,
            vars: vars
        }
    } only %}

    {# Blocks #}
    {{ entry.blocks.render({
        vars: vars
    }) }}

{% endblock %}
