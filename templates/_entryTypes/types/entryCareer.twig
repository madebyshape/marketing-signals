{% extends '_layouts/global' %}

{% block site %}

    {# Career Hero #}

    {% include '_components/heroCareer' with {
        params: {
            entry: entry,
            vars: vars
        }
    } only %}

    {# Longform #}

    {% include '_components/longform' with {
        params: {
            entry: entry,
            vars: vars,
            id: 'career-content',
            shareLabel: 'Share this career'
        }
    } only %}

{% endblock %}
