{% extends '_layouts/global' %}

{% block site %}

    {# Career Hero #}

    {% include '_components/heroCareer' with {
        params: {
            entry: entry,
            vars: vars
        }
    } only %}

    {# Longform Career Post #}

    {# The Apply Button's target, clear of the fixed header (ADR-0001). #}

    <div id="career-content" class="scroll-mt-20 | xl:scroll-mt-[123px]" tabindex="-1"></div>

{% endblock %}
