{% extends '_layouts/global' %}

{% block site %}

    {# Blog Hero #}

    {% include '_components/heroBlog' with {
        params: {
            entry: entry,
            vars: vars
        }
    } only %}

    {# Longform #}

    {% include '_components/longform' with {
        params: {
            entry: entry,
            vars: vars,
            id: 'blog-content'
        }
    } only %}

{% endblock %}
