{# The Case Studies page renders exactly as any other page, so it includes the Entry - Blocks
   type template rather than repeating the Hero and Blocks rendering here. The list of case
   studies is a later Block. #}

{% include '_pages/types/entryBlocks' %}
