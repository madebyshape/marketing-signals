{% extends '_layouts/global' %}

{% block site %}

    {{ entry.hero.render({
        vars: vars
    }) }}

    {# Rendered as one call rather than a loop so a second Block never re-renders the page. #}

    {{ entry.blocks.render({
        vars: vars
    }) }}

{% endblock %}
