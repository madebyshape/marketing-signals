{% extends '_layouts/global' %}

{% block site %}

    {{ entry.hero.render({
        vars: vars
    }) }}

    {# Rendering the Blocks as one call rather than looping over them keeps a second Block from
       rendering the whole page a second time. #}

    {{ entry.blocks.render({
        vars: vars
    }) }}

{% endblock %}
