{% extends '_layouts/global' %}

{% block site %}

    {{ entry.hero.render({
        vars: vars
    }) }}

    {# Brand Guidelines Blocks #}

    {{ entry.blocks.render({
        vars: vars
    }) }}

{% endblock %}
